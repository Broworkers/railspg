%div.line{ class: message.classes }
  %time= l message.created_at, :format => :short
  %span.sender
    - if message.scope
      %span.scope= message.scope
    - if message.name
      %span.name= message.name
  %span.body
    :markdown
      #{message.body}
